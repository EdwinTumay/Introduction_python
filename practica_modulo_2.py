""" 
1. Crea una clase `Animal` con los atributos `nombre` y `edad`, y un método `hacer_sonido`.
2. Crea una subclase `Perro` que hereda de `Animal`, añade el atributo `raza`, y 
    sobreescribe el método `hacer_sonido`.
3. Crea una subclase `Gato` que hereda de `Animal`, añade el atributo `color`, y 
    sobreescribe el método `hacer_sonido`.
4. Crea instancias de `Perro` y `Gato` y haz que emitan sus sonidos.
5. Crea una función `presentar_animal` que reciba un `Animal` y muestre su nombre, 
    edad, y haga que el animal emita su sonido. Utilízala con instancias de `Perro` y `Gato`. 
    Observa cómo se aplica el polimorfismo."""



